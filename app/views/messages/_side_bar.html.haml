.wrapper__side-bar
  .side-bar__user-name
    .user-name__box
      .box__name
        = current_user.name
      .box__icon
      = link_to new_group_path do
        = icon('far', 'edit', class: 'icon1_btn')
      = link_to edit_user_path(current_user) do
        = icon('fas', 'cog')
  .side-bar__group-list
    - current_user.groups.each do |group|
      .group-list__group1
        = link_to '#' do
          .group1-name
            = group.name
          .group1-latestmessage
            まだメッセージがありません
    